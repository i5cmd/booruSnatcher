<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Kartian_s_Booru_Snatcher.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Kartian_s_Booru_Snatcher"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Kartian's Booru Snatcher">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBox PlaceholderText="Tags..." Margin="20 10" x:Name="TagsTextBox" VerticalAlignment="Center" Width="600"></TextBox>
            <Button Content="Snatch!" Click="Button_Click" VerticalAlignment="Center"></Button>
        </StackPanel>
        <GridView Grid.Row="1" x:Name="Images" ItemsSource="{x:Bind imageDatas}" ShowsScrollingPlaceholders="True" IsItemClickEnabled="True" ItemClick="Images_ItemClick">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="local:ImageData">
                    <Image Stretch="UniformToFill" Source="{x:Bind SampleUrl}" Width="100" Height="100" Tag="{x:Bind PostId}"></Image>
                </DataTemplate>
            </GridView.ItemTemplate>
            <GridView.Footer>
                <StackPanel x:Name="Buttons" Orientation="Horizontal" HorizontalAlignment="Center">
                </StackPanel>
            </GridView.Footer>
        </GridView>
    </Grid>
</Window>
